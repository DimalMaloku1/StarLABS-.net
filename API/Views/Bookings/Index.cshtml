@using Application.DTOs;
@model IEnumerable<BookingDto>
@{
    ViewData["Title"] = "List of Bookings";
}

    <div class="crudIndex">
        <div class="buttons-title">
            <h3>Bookings</h3>
            <a asp-controller="Bookings" asp-action="Create" class="btn btn-success"><span class="material-icons">add_circle</span> Add New Booking</a>
        </div>
    <table class="table table-striped">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.User.Email)</th>
                    <th>@Html.DisplayNameFor(model => model.CheckInDate)</th>
                    <th>@Html.DisplayNameFor(model => model.CheckOutDate)</th>
                    @* <th>@Html.DisplayNameFor(model => model.TotalPrice)</th>
                    <th>@Html.DisplayNameFor(model => model.Room.RoomNumber)</th> *@
                     <th>@Html.DisplayNameFor(model => model.CreatedAt)</th> 
                     <th>@Html.DisplayNameFor(model => model.UpdatedAt)</th> 

                        <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.User.Email)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.CheckInDate)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.CheckOutDate)
                        </td>
                       @*  <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.TotalPrice)
                        </td> 
                         <td class="align-middle">
                             @Html.DisplayFor(modelItem => item.Room.RoomNumber)
                    </td>*@
                        <td class="align-middle">
                             @Html.DisplayFor(modelItem => item.CreatedAt) 
                        </td><td class="align-middle">
                             @Html.DisplayFor(modelItem => item.UpdatedAt) 
                        </td>
                            <td class="align-middle">
                        <a class="btn btn-outline-primary" asp-controller="Bookings" asp-action="Edit" asp-route-id="@item.Id"><span class="material-icons">edit</span></a>
                        <a class="btn btn-outline-info" asp-controller="Bookings" asp-action="Details" asp-route-id="@item.Id"><span class="material-icons">info</span></a>
                        <a class="btn btn-danger" asp-controller="Bookings" asp-action="Delete" asp-route-id="@item.Id"><span class="material-icons">delete</span></a>
                            </td>

                    </tr>
                }
            </tbody>

        </table>
    </div>


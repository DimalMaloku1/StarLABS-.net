@model IEnumerable<Application.DTOs.RoomDto>
@Html.Partial("_Navbar")
<div class="relative mx-auto overflow-x-auto">
    <div class="card w-2/3 mx-auto mt-12">
        <div class="card-body">
            <table class="datatable table table-bordered table-striped table-hover py-4">
                <thead class="table-dark ">
                <th> Room Id </th>
                <th> Room Number</th>
                <th> Room Type </th>
                <th> Created At</th>
                <th> Actions</th>
                </thead>
                <tbody>
                @foreach (var room in Model)
                {
                    <tr>
                        <td> @room.Id </td>
                        <td> @room.RoomNumber</td>
                        <td> @room.Type</td>
                        <td> @room.CreatedAt</td>
                        <td class="text-center ">
                            <a class=" btn btn-info" asp-controller="Room" asp-action="Details" asp-route-id="@room.Id">Details</a>
                            <a class=" btn btn-primary" asp-controller="Room" asp-action="Edit" asp-route-id="@room.Id">Edit</a>
                            <form class="" asp-action="Delete" asp-controller="Room" asp-route-id="@room.Id" method="post" style="display:inline;">
                                <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this item?');">Delete</button>
                            </form>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="//cdn.datatables.net/1.13.7/js/jquery.dataTables.min.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/admin-lte@3.2/dist/js/adminlte.min.js"></script>
    <script src="../wwwroot/js/site.js"></script>
</div>